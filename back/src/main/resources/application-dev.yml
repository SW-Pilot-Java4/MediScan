spring:
  output:
    ansi:
      enabled: always
      
#  datasource:
#    url: jdbc:mysql://127.0.0.1:5432
#    driver-class-name: org.postgresql.Driver
#    username: member
#    password: mediscan

  datasource:
    url: jdbc:postgresql://localhost:5432/mediscan
    driver-class-name: org.postgresql.Driver
    username: member
    password: mediscan

#  jpa:
#    hibernate:
#      ddl-auto: update
#    show-sql: true
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.PostgreSQLDialect


  jpa:
    hibernate:
      ddl-auto: update
    properties:
      default_batch_fetch_size: 100
      format_sql: true
      highlight_sql: true
      use_sql_comments: true
    show-sql: true


  batch:
    initialize-schema: always
    job:
      enabled: false
      name: loadHospitalJob



logging:
  level:
    com.ms.back: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
    org.hibernate.orm.jdbc.extract: TRACE
    org.springframework.transaction.interceptor: TRACE
